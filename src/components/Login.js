import { Button } from 'antd';
import React from 'react';

const Login: React.FC = () => (
    <>
        <Button style={{marginLeft: '520px'}} type="primary">登录</Button>
    </>
);

export default Login;